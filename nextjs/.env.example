# Browser-visible API origin (never put secrets in NEXT_PUBLIC_*).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-only Laravel origin used by Next.js route handlers.
BACKEND_URL=http://127.0.0.1:8000
