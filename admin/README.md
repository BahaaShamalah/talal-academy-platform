# لوحة إدارة طلال أكاديمي

Next.js على المنفذ `3001`. تتحدث مع Laravel عبر `API_URL` (سيرفر) و`NEXT_PUBLIC_API_URL` (أصل عام، ليس سرًا).

```bash
copy .env.example .env.local
npm install
npm run dev
```

افتح [http://localhost:3001](http://localhost:3001). الباك اند يجب أن يعمل على `:8000`.
