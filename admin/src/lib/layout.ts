/** Standard max width for admin dashboard content pages (px). */
export const ADMIN_CONTENT_MAX_WIDTH = 1024;

/** Tailwind-ready class for centered admin content shells. */
export const adminContentClass = 'mx-auto w-full max-w-[1024px]';
